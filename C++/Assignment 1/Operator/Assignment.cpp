#include<iosteam>
using namespace std;

int main()
 {
	int a = 6, b = ;

	// Assignment Operator.
	cout << "a = " << a << endl;

	//  Add Operator.
	cout << "a += b is " << (a += b) << endl;

	// Subtract Operator.
	cout << "a -= b is " << (a -= b) << endl;

	//  Multiply Operator.
	cout << "a *= b is " << (a *= b) << endl;

	//  Divide Operator.
	cout << "a /= b is " << (a /= b);

	return 0;
}